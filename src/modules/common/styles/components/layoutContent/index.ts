import { StyleSheet } from 'react-native';
import { spacing } from '../../../utils/theme';

export const layaoutContentStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: spacing.m,
  },
});
