import { StyleSheet } from 'react-native';
import { colors } from '../../../utils/theme';

export const buttonCreateStyles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    backgroundColor: colors.whiteLight,
    borderRadius: 15,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
